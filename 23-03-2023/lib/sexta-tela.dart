import 'package:flutter/material.dart';

class SextaTela extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sexta Tela'),
      ),
    );
  }
}
